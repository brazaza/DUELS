# Duels 🤠

Браузерная 2D игра-дуэль с распознаванием жестов через веб-камеру.

## Описание

Два игрока смотрят в камеру, показывают "палец вверх" 👍 и по сигналу "ОГОНЬ!" сгибают палец вниз, чтобы "выстрелить". Побеждает тот, кто быстрее среагировал.

### Режимы игры

1. **Тренировка** — локальный режим без сервера
2. **Дуэль** — мультиплеер 1v1 по ссылке

## Технологии

- **Монорепо**: pnpm workspace
- **Клиент**: Vite + React + TypeScript
- **Сервер**: Node.js + WebSocket (ws)
- **Жесты**: MediaPipe Hands (в браузере)
- **Видео**: WebRTC (peer-to-peer)

## Быстрый старт

### Требования

- Node.js 18+
- pnpm 9+

### Установка

```bash
# Клонировать репозиторий
cd DUELS

# Установить зависимости
pnpm install

# Запустить в режиме разработки
pnpm dev
```

### Запуск по отдельности

```bash
# Только клиент (тренировка работает без сервера)
pnpm dev:client

# Только сервер
pnpm dev:server
```

## Режим тренировки (localhost)

1. Запустить `pnpm dev:client`
2. Открыть http://localhost:5173
3. Нажать "Тренировка"
4. Разрешить доступ к камере
5. Показать 👍 в камеру
6. По сигналу "ОГОНЬ!" согнуть палец

## Режим дуэли

1. Запустить `pnpm dev` (клиент + сервер)
2. Открыть http://localhost:5173
3. Нажать "Дуэль"
4. Создать комнату или ввести код
5. Поделиться ссылкой с другом
6. Оба игрока нажимают "Готов"
7. Дождаться сигнала и выстрелить!

## Структура проекта

```
DUELS/
├── apps/
│   ├── client/         # React приложение
│   │   ├── src/
│   │   │   ├── components/   # UI компоненты
│   │   │   ├── fsm/          # Машины состояний
│   │   │   ├── hooks/        # React хуки
│   │   │   ├── modules/      # Камера, жесты, звук
│   │   │   └── i18n/         # Переводы
│   │   └── ...
│   └── server/         # WebSocket сервер
│       └── src/
│           ├── roomManager.ts    # Управление комнатами
│           ├── gameHandler.ts    # Логика игры
│           └── signalingHandler.ts # WebRTC сигнализация
└── packages/
    └── shared/         # Общие типы
        └── src/
            ├── types.ts      # FSM, игроки, комнаты
            ├── protocol.ts   # WS протокол
            └── config.ts     # Конфигурация
```

## Конфигурация

Файл: `packages/shared/src/config.ts`

| Параметр | Значение | Описание |
|----------|----------|----------|
| `DRAW_DELAY_MIN` | 2000 мс | Мин. задержка перед ОГОНЬ |
| `DRAW_DELAY_MAX` | 5000 мс | Макс. задержка перед ОГОНЬ |
| `COUNTDOWN_DURATION` | 3000 мс | Длительность отсчёта |
| `WS_PORT` | 3001 | Порт WebSocket сервера |

## Переменные окружения

### Клиент (`apps/client/.env`)

```
VITE_WS_URL=ws://localhost:3001
```

### Сервер (`apps/server/.env`)

```
WS_PORT=3001
```

## Мобильные устройства

- Используется фронтальная камера
- Видео зеркалится автоматически
- UI адаптирован под вертикальную ориентацию

## Лицензия

MIT
