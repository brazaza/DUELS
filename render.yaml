services:
  - type: web
    name: duels-server
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install
    startCommand: cd apps/server && pnpm start
    envVars:
      - key: NODE_VERSION
        value: 20
